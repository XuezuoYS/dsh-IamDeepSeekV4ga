# DeepSeek Harness 组合包（bundle）patch 层。
# 当该包被加入 profile 的 dsh.profile.bundles 后，这一层会被应用。
# 注意：插件行按「包名」引用，Node 模块解析会找到已安装的代码（main: lib/index.js）。
- insert:
    - id: iam-deepseek-v4ga
      name: dsh-plugin-iam-deepseek-v4ga
